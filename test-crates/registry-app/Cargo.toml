[package]
name = "registry-app"
version = "0.1.0"
edition = "2024"
publish = false

[dependencies]
qubit-reflect = { path = "../.." }
registry-impl-a = { path = "../registry-impl-a" }
registry-impl-b = { path = "../registry-impl-b" }
registry-types = { path = "../registry-types" }
